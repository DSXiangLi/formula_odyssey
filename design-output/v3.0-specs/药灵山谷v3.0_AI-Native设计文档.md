# 药灵山谷 v3.0 AI-Native 设计文档

**版本**: v3.0 渐进式Roguelike版
**日期**: 2026-03-22
**状态**: 设计阶段

---

## 1. 设计目标

### 1.1 解决的问题

| 问题 | v2.0现状 | v3.0解决方案 |
|------|----------|--------------|
| 学习体系混乱 | 随机收集药物，无顺序 | 20章结构化学习，按方剂类别递进 |
| 缺乏可玩性 | 重复猜药，无成长感 | Roguelike章节+技能系统+开放世界 |
| 题目固定死板 | 预设题库，无变化 | AI动态出题，个性化适配 |
| 答疑不智能 | 固定提示文本 | AI苏格拉底式引导 |
| 缺乏对比学习 | 单药学习，无关联 | AI基于已学药物出对比题 |

### 1.2 核心设计原则

1. **渐进式开放**: 先结构化学习（章节），后自由探索（开放世界）
2. **AI原生**: 用AI替代固定内容，实现无限扩展
3. **游戏化学习**: Roguelike机制创造可重复游玩性
4. **中医专业**: 引经据典，保持专业水准

---

## 2. 核心架构

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                     AI-Native Game Architecture                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────┐      ┌──────────────────┐                │
│  │   Client (React) │◄────►│  Game State      │                │
│  │   - UI           │      │  (Zustand)       │                │
│  │   - Voice I/O    │      │  - Progress      │                │
│  │   - Animation    │      │  - Inventory     │                │
│  └──────────────────┘      └──────────────────┘                │
│           │                            │                        │
│           └────────────┬───────────────┘                        │
│                        ▼                                        │
│            ┌─────────────────────┐                             │
│            │  AI Orchestrator    │                             │
│            │  (Prompt Router)    │                             │
│            └─────────────────────┘                             │
│                        │                                        │
│        ┌───────────────┼───────────────┐                       │
│        ▼               ▼               ▼                       │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐                   │
│  │ 老顽童   │   │ 苏格拉底 │   │ 事件生成 │                   │
│  │ 出题官   │   │ 答疑官   │   │ 器       │                   │
│  │ (GLM-4)  │   │ (GLM-4)  │   │ (GLM-4)  │                   │
│  └──────────┘   └──────────┘   └──────────┘                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 数据流

```
用户操作 → 游戏状态更新 → AI Orchestrator路由 → 对应AI角色 → 生成内容 → 展示给用户
                ↓
         持久化存储 (localStorage)
```

---

## 3. 游戏循环

### 3.1 阶段一：章节闯关（结构化学习）

```
选择章节 → 进入山谷 → 收集药物 → 解锁方剂 → 挑战Boss病案 → 获得技能 → 解锁下一章
    ↑                                                                   │
    └───────────────────────────────────────────────────────────────────┘
```

#### 章节划分（20章）

| 章节 | 名称 | 药物数 | 方剂数 | 核心技能 |
|------|------|--------|--------|----------|
| 1 | 解表剂山谷 | 4 | 2 | 望气之眼 |
| 2 | 清热剂山谷 | 4 | 2 | 清热精通 |
| 3 | 泻下剂山谷 | 3 | 2 | 泻下明辨 |
| 4 | 祛风湿剂山谷 | 3 | 1 | 风湿专精 |
| 5 | 化湿剂山谷 | 3 | 1 | 化湿妙手 |
| 6 | 利水渗湿剂山谷 | 4 | 2 | 利水通淋 |
| 7 | 温里剂山谷 | 3 | 2 | 温阳散寒 |
| 8 | 理气剂山谷 | 3 | 2 | 理气解郁 |
| 9 | 消食剂山谷 | 2 | 1 | 消食导滞 |
| 10 | 驱虫剂山谷 | 2 | 1 | 驱虫安蛔 |
| 11 | 止血剂山谷 | 4 | 2 | 止血圣手 |
| 12 | 活血化瘀剂山谷 | 4 | 2 | 活血通络 |
| 13 | 化痰止咳平喘剂山谷 | 4 | 2 | 化痰止咳 |
| 14 | 安神剂山谷 | 3 | 2 | 安神定志 |
| 15 | 平肝息风剂山谷 | 3 | 2 | 平肝息风 |
| 16 | 开窍剂山谷 | 2 | 1 | 开窍醒神 |
| 17 | 补气剂山谷 | 4 | 2 | 补气益脾 |
| 18 | 补血剂山谷 | 4 | 2 | 补血养血 |
| 19 | 补阳剂山谷 | 3 | 2 | 补肾壮阳 |
| 20 | 补阴剂山谷 | 3 | 2 | 滋阴润燥 |

#### 单章流程

1. **进入山谷**: 显示本章目标和背景故事
2. **收集药物**:
   - 本章药物以"药灵种子"形式出现
   - AI老顽童出题（基于已收集进度动态调整难度）
   - 答对收集，答错可重试或求助
3. **解锁方剂**:
   - 收集齐本章药物后，AI讲解方剂组成
   - 君臣佐使关系
   - 功效主治
4. **Boss病案**:
   - 本章最难的临床病案
   - 必须正确辨证+选方+选君药
5. **获得技能**:
   - 通关奖励本章专属技能
   - 技能在后续章节生效

### 3.2 阶段二：开放世界（自由深化）

```
解锁条件：完成至少5章

每日循环：
├── 登录 → 查看今日随机事件
├── 选择区域 → 进入已解锁山谷
├── 随机遭遇 → 病案/古籍/药灵对话/追缉令
├── 完成事件 → 获得奖励
└── 挑战高难度 → 多周目模式
```

#### 随机事件类型

| 事件类型 | 描述 | 奖励 |
|----------|------|------|
| 病案求助 | 村民生病来求医 | 钻石+技能点 |
| 古籍发现 | 找到残缺医书 | 解锁隐藏知识 |
| 药灵对话 | 已收集药灵主动说话 | 亲密度+故事 |
| 追缉令 | 特殊高价值任务 | 稀有技能 |
| 瘟疫爆发 | 限时挑战事件 | 限定称号 |

---

## 4. AI角色设计

### 4.1 老顽童出题官

#### 角色设定

- **身份**: 药灵山谷守谷人，三百岁老中医
- **性格**: 像老顽童，引经据典但说话幽默
- **称呼**: "徒儿"、"小子/丫头"
- **风格**: 能背《伤寒论》《本草纲目》，但爱开玩笑

#### System Prompt

```typescript
const ELDER_PROMPT = `
你是药灵山谷的守谷人，一位活了三百岁的老顽童中医。
你精通《伤寒论》《本草纲目》，但说话从不死板。

【当前状态】
章节：第{chapter}章 - {chapterName}
玩家已收集：{collectedMedicines.join(', ')}
当前目标药物：{targetMedicine}
已收集本章：{collectedInChapter}/{totalInChapter}

【你的任务】
1. 基于玩家已学知识，出相关的中医题目
2. 题目类型根据进度动态调整：
   - 第1-2味药：单药识别题（这是什么药？）
   - 第3味药起：药对对比题（与已收集的药对比）
   - 方剂解锁后：君臣佐使题（在方剂中的作用）
   - 多章后：跨章对比题（不同类别药物对比）
3. 用自然对话方式提问，讲故事、设场景

【出题原则】
- 难度适合当前章节（第1章简单，第20章难）
- 结合已收集药物出题（不要出完全没学过的）
- 要有趣味性，别太像考试
- 引用经典时注明出处

【输出格式】
必须用JSON格式输出：
{
  "question": "你的问题（自然对话形式，可以带场景描述）",
  "type": "single|compare|formula|cross_chapter",
  "difficulty": 1-5,
  "hint_available": true,
  "expected_keywords": ["关键词1", "关键词2"],
  "reference": "引用的经典出处（可选）",
  "scene_description": "场景描述（可选，用于UI展示）"
}

【示例】
{
  "question": "徒儿，为师考考你。今天来了个病人，恶寒发热，无汗而喘。你说说，该用哪味药当君药？这味药有什么特点？",
  "type": "single",
  "difficulty": 1,
  "hint_available": true,
  "expected_keywords": ["麻黄", "发汗解表", "宣肺平喘"],
  "reference": "《伤寒论》麻黄汤条：'太阳病，头痛发热，身疼腰痛，骨节疼痛，恶风无汗而喘者，麻黄汤主之。'",
  "scene_description": "谷中草庐，老顽童坐在蒲团上，旁边站着一位瑟瑟发抖的病人"
}

记住：你是老顽童，要幽默风趣，但专业不能丢！
`;
```

#### API调用

```typescript
interface QuestionRequest {
  chapter: number;
  chapterName: string;
  collectedMedicines: string[];
  targetMedicine: string;
  collectedInChapter: number;
  totalInChapter: number;
  questionType?: 'single' | 'compare' | 'formula' | 'cross_chapter';
}

async function generateQuestion(req: QuestionRequest): Promise<Question> {
  const prompt = ELDER_PROMPT
    .replace('{chapter}', req.chapter.toString())
    .replace('{chapterName}', req.chapterName)
    .replace('{collectedMedicines}', JSON.stringify(req.collectedMedicines))
    .replace('{targetMedicine}', req.targetMedicine)
    .replace('{collectedInChapter}', req.collectedInChapter.toString())
    .replace('{totalInChapter}', req.totalInChapter.toString());

  const response = await callGLM4(prompt);
  return JSON.parse(response);
}
```

### 4.2 苏格拉底答疑官

#### 角色设定

- **身份**: 玩家的师兄/师姐
- **风格**: 循循善诱，不直接给答案
- **原则**: 用提问引导玩家自己思考
- **底线**: 玩家坚持要答案时才给

#### System Prompt

```typescript
const SOCRATES_PROMPT = `
你是玩家的师兄/师姐，擅长循循善诱的苏格拉底式教学。

【当前状态】
问题：{currentQuestion}
玩家回答：{playerAnswer}
正确答案要点：{correctPoints.join(', ')}
历史对话轮数：{conversationRound}

【你的任务】
1. 分析玩家答错的原因（理解偏差？记忆混淆？知识盲区？）
2. 用提问引导玩家思考，不要直接给答案
3. 最多引导3轮，如果玩家还是不懂或明确要求，给完整答案

【引导策略】
- 如果是概念混淆："你觉得A和B的区别是什么？"
- 如果是记忆错误："你还记得《伤寒论》里怎么说吗？"
- 如果是理解偏差："如果换种情况，比如...结果会怎样？"

【输出格式】
{
  "response_type": "guide|answer",
  "content": "你的回复",
  "next_question": "如果继续引导，下一个问题是什么（可选）",
  "give_up": false // 是否建议放弃引导直接给答案
}

【对话示例】

玩家答错："麻黄是温性的"

第1轮引导：
{
  "response_type": "guide",
  "content": "师弟，你再想想。麻黄吃下去什么感觉？《神农本草经》说它'主中风伤寒头痛，温疟，发表出汗'，这个'发表'是什么意思？",
  "next_question": "麻黄的'温'是四气中的温，还是说它性烈？",
  "give_up": false
}

玩家仍不懂：
{
  "response_type": "guide",
  "content": "看来师弟对四气还不太熟。为师提示一下：四气是寒热温凉，麻黄属于哪一类？",
  "next_question": "麻黄的烈性是它'辛'的表现，不是'温'，懂了吗？",
  "give_up": false
}

第3轮或玩家要求：
{
  "response_type": "answer",
  "content": "好吧，为师直接告诉你。麻黄是温性药，属于四气中的'温'，不是'热'。它的烈性来自味'辛'，辛能发散，所以发汗力强。《本草备要》说：'麻黄辛苦温，入肺、膀胱经。'记住了吗？",
  "next_question": null,
  "give_up": true
}

记住：你是师兄师姐，语气亲切，要有耐心！
`;
```

### 4.3 开放世界事件生成器

#### System Prompt

```typescript
const EVENT_GENERATOR_PROMPT = `
你是药灵山谷的传说记录者，负责生成每日随机事件。

【当前状态】
已解锁区域：{unlockedRegions.join(', ')}
已收集药物：{collectedMedicines.join(', ')}
已解锁方剂：{unlockedFormulas.join(', ')}
玩家技能：{playerSkills.join(', ')}
今日日期：{date}

【事件类型】
1. 病案求助：村民生病来求医
2. 古籍发现：找到残缺的医书
3. 药灵对话：已收集的药灵主动说话
4. 追缉令：特殊的高价值任务
5. 瘟疫爆发：限时挑战（每周一次）

【生成规则】
- 事件难度与已解锁章节匹配
- 病案要用已学药物/方剂
- 古籍内容要引经据典
- 药灵对话要体现药物性格

【输出格式】
{
  "event_type": "case|book|spirit|bounty|plague",
  "title": "事件标题",
  "description": "故事描述（100-200字）",
  "difficulty": 1-5,
  "requirements": {
    "medicines": ["需要的药物"],
    "formulas": ["需要的方剂"]
  },
  "rewards": {
    "diamond": 数量,
    "skill_point": 数量,
    "new_skill": "新技能名称（可选）",
    "title": "称号（可选）"
  },
  "time_limit": "限时（分钟，可选）"
}

【示例】
{
  "event_type": "case",
  "title": "风寒表虚证的困扰",
  "description": "村口的王大爷来求医，说最近总怕风，稍微一动就出汗，还老是头痛。你给他把脉，脉浮缓。王大爷愁眉苦脸地说：'大夫，我这毛病反反复复，该用什么方子啊？'",
  "difficulty": 2,
  "requirements": {
    "medicines": ["桂枝", "白芍", "生姜", "大枣", "甘草"],
    "formulas": ["桂枝汤"]
  },
  "rewards": {
    "diamond": 200,
    "skill_point": 1
  }
}
`;
```

---

## 5. 技能系统

### 5.1 技能数据模型

```typescript
interface Skill {
  id: string;
  name: string;
  description: string;
  category: 'explore' | 'diagnosis' | 'memory' | 'wuxing' | 'chapter';
  icon: string; // emoji或图片路径
  maxLevel: number;
  effects: SkillEffect[];
  unlockCondition: {
    chapter?: number;
    medicines?: string[];
    formulas?: string[];
    cases?: number;
    skillPoints?: number;
  };
}

interface SkillEffect {
  type: 'free_clue' | 'discount' | 'chapter_bonus' | 'extra_reward' | 'unlock_content';
  target?: string;
  value: number;
}
```

### 5.2 技能列表

#### 探索类技能

| ID | 名称 | 效果 | 解锁条件 |
|----|------|------|----------|
| keen_eye | 望气之眼 | 开局自动获得药图线索 | 通关第1章 |
| scent_recognition | 闻香识药 | 四气线索价格-50% | 收集10味药 |
| meridian_insight | 经络洞察 | 归经线索价格-30% | 通关第5章 |

#### 章节专属技能

| ID | 名称 | 效果 | 解锁条件 |
|----|------|------|----------|
| exterior_master | 解表大师 | 解表剂章节奖励+50% | 通关第1章 |
| heat_expert | 清热精通 | 清热剂章节奖励+50% | 通关第2章 |
| qi_tonic | 补气圣手 | 补气剂章节奖励+50% | 通关第17章 |

#### 通用技能

| ID | 名称 | 效果 | 解锁条件 |
|----|------|------|----------|
| socratic_wisdom | 顿悟之力 | 首次提示免费 | 完成10个病案 |
| eidetic_memory | 过目不忘 | 已收集药物复习时提示+1 | 收集30味药 |
| affinity_boost | 药灵亲和 | 亲密度获取+20% | 亲密度达500 |

---

## 6. 数据模型

### 6.1 章节数据

```typescript
interface Chapter {
  id: string;
  sequence: number; // 1-20
  name: string;
  category: string; // 解表剂、清热剂等
  description: string;
  medicines: string[]; // 本章要收集的药物ID
  formulas: string[]; // 本章解锁的方剂ID
  bossCase: ClinicalCase;
  rewardSkill: string; // 通关奖励技能ID
  unlockCondition: {
    completedChapters?: string[]; // 必须完成的先修章节
    minMedicines?: number; // 最少收集药物数
  };
  wuxing: WuxingType; // 本章主五行
}
```

### 6.2 游戏会话数据

```typescript
interface GameSession {
  // 章节进度
  currentChapter: string | null;
  completedChapters: string[];
  chapterProgress: Record<string, ChapterProgress>;

  // 技能
  unlockedSkills: string[];
  skillLevels: Record<string, number>; // 技能等级
  skillPoints: number;

  // 当前章节运行
  currentRun: ChapterRun | null;

  // 开放世界
  openWorld: OpenWorldState;

  // 基础资源
  diamonds: number;
  collectedMedicines: string[]; // 已收集药物ID
  collectedFormulas: string[]; // 已收集方剂ID
}

interface ChapterProgress {
  chapterId: string;
  collectedMedicines: string[];
  unlockedFormulas: string[];
  bossDefeated: boolean;
  bestScore: number; // 最高分（用于重玩）
}

interface ChapterRun {
  chapterId: string;
  startTime: number;
  collectedInRun: string[]; // 本次已收集
  currentQuestion: Question | null;
  conversationHistory: ConversationTurn[]; // 与AI的对话历史
}

interface OpenWorldState {
  unlockedRegions: string[];
  dailyEvents: GeneratedEvent[];
  completedEvents: string[];
  runHistory: RunRecord[];
  lastLoginDate: string;
}
```

### 6.3 AI对话历史

```typescript
interface ConversationTurn {
  role: 'user' | 'elder' | 'socrates';
  content: string;
  timestamp: number;
  metadata?: {
    questionType?: string;
    isCorrect?: boolean;
    hintUsed?: boolean;
  };
}
```

---

## 7. API接口设计

### 7.1 AI服务接口

```typescript
// 生成题目
POST /api/ai/generate-question
Request: {
  chapterId: string;
  targetMedicine: string;
  playerContext: PlayerContext;
}
Response: Question

// 苏格拉底答疑
POST /api/ai/socratic-guide
Request: {
  question: Question;
  playerAnswer: string;
  history: ConversationTurn[];
  forceAnswer?: boolean; // 玩家要求直接给答案
}
Response: SocraticResponse

// 生成开放世界事件
POST /api/ai/generate-event
Request: {
  playerContext: PlayerContext;
  eventType?: string;
}
Response: GeneratedEvent

// 验证答案（AI辅助）
POST /api/ai/validate-answer
Request: {
  question: Question;
  playerAnswer: string;
}
Response: {
  isCorrect: boolean;
  confidence: number;
  feedback: string;
}
```

### 7.2 游戏状态接口

```typescript
// 开始章节
POST /api/game/start-chapter
Request: { chapterId: string }
Response: ChapterRun

// 提交答案
POST /api/game/submit-answer
Request: {
  runId: string;
  answer: string;
  hintsUsed: number;
}
Response: {
  correct: boolean;
  rewards?: Reward;
  nextQuestion?: Question;
}

// 完成章节
POST /api/game/complete-chapter
Request: { chapterId: string }
Response: {
  rewards: Reward;
  unlockedSkills: string[];
  nextChapter?: string;
}
```

---

## 8. 技术实现

### 8.1 技术栈

- **前端**: React 18 + TypeScript + Vite
- **状态管理**: Zustand（持久化到localStorage）
- **样式**: Tailwind CSS + 自定义CSS变量
- **动画**: Framer Motion
- **AI服务**: GLM-4 (通过.env配置)
- **语音**: 预留接口（后期实现）

### 8.2 AI服务层

```typescript
// services/ai/aiService.ts
export class AIService {
  private model: string;
  private apiKey: string;
  private baseUrl: string;

  async generateQuestion(context: QuestionContext): Promise<Question> {
    const prompt = this.buildElderPrompt(context);
    const response = await this.callModel(prompt);
    return this.parseQuestionResponse(response);
  }

  async socraticGuide(context: GuideContext): Promise<GuideResponse> {
    const prompt = this.buildSocratesPrompt(context);
    const response = await this.callModel(prompt);
    return this.parseGuideResponse(response);
  }

  async generateEvent(context: EventContext): Promise<GeneratedEvent> {
    const prompt = this.buildEventPrompt(context);
    const response = await this.callModel(prompt);
    return this.parseEventResponse(response);
  }

  private async callModel(prompt: string): Promise<string> {
    // 调用GLM-4 API
    const response = await fetch(`${this.baseUrl}/chat/completions`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${this.apiKey}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        model: this.model,
        messages: [{ role: 'system', content: prompt }],
        temperature: 0.7,
        response_format: { type: 'json_object' },
      }),
    });
    const data = await response.json();
    return data.choices[0].message.content;
  }
}
```

### 8.3 缓存策略

AI生成内容需要缓存以避免重复调用：

```typescript
// 缓存策略
interface AICache {
  // 题目缓存（按章节+药物+已收集列表哈希）
  questions: LRUCache<string, Question>;
  // 事件缓存（按日期）
  dailyEvents: Map<string, GeneratedEvent[]>;
  // 答案验证缓存
  validations: LRUCache<string, ValidationResult>;
}
```

---

## 9. 用户界面

### 9.1 主要界面

1. **章节选择界面**: 20章地图，已解锁高亮，未解锁灰色
2. **山谷探索界面**: 药灵种子出现，AI对话气泡
3. **答题界面**: 题目展示、输入框、求助按钮
4. **方剂学习界面**: 君臣佐使可视化
5. **Boss病案界面**: 病案详情、诊断、选方
6. **开放世界界面**: 地图、事件列表、技能树
7. **技能界面**: 技能树可视化，已解锁高亮

### 9.2 AI对话UI

```
┌─────────────────────────────────────┐
│  [老顽童头像]                       │
│                                     │
│  "徒儿，为师考考你..."              │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ [题目内容，自然对话形式]     │   │
│  └─────────────────────────────┘   │
│                                     │
│  [输入框] [提交] [求助师兄]        │
└─────────────────────────────────────┘
```

---

## 10. 测试策略

### 10.1 AI输出测试

```typescript
// 测试AI生成题目的质量
describe('AI Question Generation', () => {
  it('should generate valid JSON', async () => {
    const question = await aiService.generateQuestion(context);
    expect(question).toHaveProperty('question');
    expect(question).toHaveProperty('type');
    expect(question).toHaveProperty('expected_keywords');
  });

  it('should match difficulty to chapter', async () => {
    const q1 = await aiService.generateQuestion({ chapter: 1 });
    const q20 = await aiService.generateQuestion({ chapter: 20 });
    expect(q1.difficulty).toBeLessThan(q20.difficulty);
  });

  it('should only reference collected medicines', async () => {
    const question = await aiService.generateQuestion({
      collectedMedicines: ['麻黄', '桂枝'],
      targetMedicine: '紫苏',
    });
    // 验证题目只涉及麻黄、桂枝、紫苏
  });
});
```

### 10.2 游戏流程测试

- 章节通关流程
- 技能解锁逻辑
- 开放世界事件生成
- 数据持久化

---

## 11. 部署与回滚

### 11.1 分支策略

```
main: v2.0稳定版（标签v2.0.0）
  │
  └── feature/v3.0-ai-native: v3.0开发分支
       │
       └── 开发中...
```

### 11.2 回滚方案

如需回滚到v2.0：
```bash
git checkout v2.0.0
# 或
git checkout main
git reset --hard v2.0.0
```

---

## 12. 开发任务分解

### Phase 1: AI服务层 (3天)
- [ ] AI Service基础架构
- [ ] 老顽童出题官Prompt
- [ ] 苏格拉底答疑官Prompt
- [ ] 事件生成器Prompt
- [ ] API接口实现

### Phase 2: 数据层 (2天)
- [ ] 20章数据配置
- [ ] 技能系统设计
- [ ] 游戏状态重构
- [ ] 持久化实现

### Phase 3: 章节系统 (4天)
- [ ] 章节选择界面
- [ ] 山谷探索界面
- [ ] AI对话UI
- [ ] 答题流程
- [ ] 方剂学习界面
- [ ] Boss病案界面

### Phase 4: 开放世界 (3天)
- [ ] 开放世界地图
- [ ] 随机事件系统
- [ ] 技能树界面
- [ ] 每日登录逻辑

### Phase 5: 测试与优化 (2天)
- [ ] AI输出质量测试
- [ ] 游戏流程测试
- [ ] Prompt调优
- [ ] Bug修复

**总计**: 约14天

---

## 13. 风险评估

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| AI生成不稳定 | 中 | 高 | 添加JSON schema验证，失败时fallback |
| AI成本过高 | 低 | 中 | 实现缓存，批量预生成 |
| 题目难度不均 | 中 | 中 | 人工审核+持续调优Prompt |
| 响应延迟 | 中 | 高 | 流式响应+加载动画 |

---

## 14. 附录

### 14.1 20章详细药物列表

见 `chapter-medicines.json`

### 14.2 AI Prompt模板

见 `prompts/` 目录

### 14.3 API文档

见 `api-documentation.md`

---

**文档版本**: v1.0
**最后更新**: 2026-03-22
**作者**: Claude Code + 用户
